name: flutter_ai_storage
description: Storage layer for Flutter AI SDK using Drift.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  drift: ^2.28.0
  flutter:
    sdk: flutter

# Generated database code is intentionally excluded from this initial skeleton.
dev_dependencies:
  drift_dev: ^2.28.0
  build_runner: ^2.5.0
